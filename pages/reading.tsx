import Layout from '@components/Layout';
import { wrapper } from '@styles/common';
import React from 'react';

function Reading() {
  return (
    <Layout>
      {' '}
      <div className={wrapper()}>Reading</div>
    </Layout>
  );
}

export default Reading;
